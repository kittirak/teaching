# **ทดสอบ performance ด้วย iperf**3

```
+----------------------+      +----------------------+
|   Client PC          |      |   Server PC          |
|  IP: 10.10.10.11     |<---> |  IP: 10.10.10.10     |
+----------------------+      +----------------------+
```



#### **0. การตั้งค่า IP Address **

ทำการตั้งค่า IP Address ให้กับเครื่องคอมพิวเตอร์ดังนี้:

- **เครื่อง Server:** ตั้งค่า IP เป็น **10.10.10.10** Subnet Mask **255.255.255.0**

- **เครื่อง Client:** ตั้งค่า IP เป็น **10.10.10.11** Subnet Mask **255.255.255.0**

### ขั้นตอนการทดลอง

#### **1. การทดสอบการเชื่อมต่อเบื้องต้นด้วย Ping**

ก่อนเริ่มการทดสอบด้วย iperf3 เราจะตรวจสอบว่าเครื่องทั้งสองสามารถสื่อสารกันได้หรือไม่

- ที่เครื่อง **Client** เปิด Command Prompt (**CMD**)
- พิมพ์คำสั่ง **ping 10.10.10.10** แล้วกด Enter
- หากการเชื่อมต่อสำเร็จ จะแสดงผลลัพธ์เป็น "Reply from 10.10.10.10..." พร้อมค่าเวลา (**Time**) และ TTL (Time To Live)
- หากไม่สำเร็จ จะแสดงผลเป็น "Request timed out" หรือ "Destination host unreachable" ให้ตรวจสอบการตั้งค่า IP และการเชื่อมต่อสาย LAN

#### **2. การติดตั้งและใช้งาน iperf3**

ดาวน์โหลดไฟล์ iperf3 ที่เป็น Zip มาแล้วแตกไฟล์

เปิด Command Prompt ที่ตำแหน่งของไฟล์ iperf3 ที่แตกออกมา

#### **3. เริ่มการทดสอบ Bandwidth**

**ขั้นที่ 1: รัน iperf3 เป็น Server**

- ที่เครื่อง **Server** เปิด Command Prompt สั่ง cd เข้าไปยังไดเรกทอรีที่เก็บไฟล์ iperf3
- พิมพ์คำสั่ง **iperf3 -s** แล้วกด Enter
- Server จะแสดงข้อความ "iperf3: listening on port 5201" ซึ่งหมายถึงพร้อมที่จะรับการเชื่อมต่อจาก Client แล้ว

**ขั้นที่ 2: รัน iperf3 เป็น Client เพื่อส่งข้อมูลไปยัง Server**

- ที่เครื่อง **Client** เปิด Command Prompt
- พิมพ์คำสั่ง **iperf3 -c 10.10.10.10** แล้วกด Enter
- Client จะเริ่มส่งข้อมูลไปยัง Server และแสดงผลลัพธ์การทดสอบออกมาทันที

**ผลลัพธ์ที่ได้**

- **Interval:** ช่วงเวลาการวัด (ปกติ 1 วินาที)

- **Transfer:** ปริมาณข้อมูลที่ถ่ายโอนในแต่ละช่วงเวลา (หน่วยเป็น MBytes หรือ GBytes)

- **Bandwidth:** ความเร็วในการส่งข้อมูลในแต่ละช่วงเวลา (หน่วยเป็น Mbits/sec หรือ Gbits/sec)

**ขั้นที่ 3: ทดสอบทิศทางย้อนกลับ (จาก Server ไป Client)**

คุณสามารถทดสอบทิศทางย้อนกลับได้โดยเพิ่มพารามิเตอร์ **-R** (Reverse) ที่เครื่อง Client

- ที่เครื่อง **Server** ยังคงรันคำสั่ง **iperf3 -s**
- ที่เครื่อง **Client** พิมพ์คำสั่ง **iperf3 -c 10.10.10.10 -R** แล้วกด Enter
- คำสั่งนี้จะสั่งให้เครื่อง Server ส่งข้อมูลกลับมาที่ Client เพื่อวัด Bandwidth ในทิศทางย้อนกลับ

#### **4. การทดสอบ UDP Bandwidth และ Jitter**

นอกจากการทดสอบ TCP (ซึ่งเป็นค่า Default) เราสามารถทดสอบด้วยโปรโตคอล **UDP** ได้ ซึ่งจะแสดงค่า Jitter และ Packet Loss

- ที่เครื่อง **Server** ยังคงรันคำสั่ง **iperf3 -s**
- ที่เครื่อง **Client** พิมพ์คำสั่ง **iperf3 -c 10.10.10.10 -u** แล้วกด Enter
- ผลลัพธ์ที่ได้จะแสดงค่า **Jitter** และ **Packet Loss** เพิ่มขึ้นมาด้วย

### 📝 สรุปและวิเคราะห์ผลลัพธ์

- **Bandwidth:** บันทึกค่า Bandwidth ที่ได้จากการทดสอบแต่ละครั้ง (จาก Client ไป Server และ Server ไป Client) และเปรียบเทียบกัน

- **Jitter และ Packet Loss:** บันทึกค่าที่ได้จากการทดสอบ UDP และพิจารณาว่ามีผลต่อคุณภาพการสื่อสารประเภท Real-time (เช่น VoIP หรือ Video Conference) อย่างไร